Small uppercase chip for tags, categories and status labels.

```jsx
<Badge tone="green">Éducation</Badge>
<Badge tone="red" soft>Presse</Badge>
```

Tones: `green` / `yellow` / `red` (flag), `ink` / `sand` / `sage` (brand). Add `soft` for a tinted low-contrast variant, `icon` for a leading glyph.
