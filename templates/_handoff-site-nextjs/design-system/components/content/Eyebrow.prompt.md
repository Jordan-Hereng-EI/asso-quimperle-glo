Eyebrow kicker label above a heading.

```jsx
<Eyebrow>Nos missions</Eyebrow>
<Eyebrow color="var(--benin-red)" ondark>Sur le terrain</Eyebrow>
```

Pairs with SectionHeading. Use `ondark` on dark bands, `color` to change the tick.
