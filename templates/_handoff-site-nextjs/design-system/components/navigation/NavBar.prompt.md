Sticky site header with logo badge, nav links and a yellow donate CTA.

```jsx
<NavBar transparent scrolled={scrolled} onCta={openDonate} />
```

Use `transparent` when overlaying the hero; drive `scrolled` from a scroll listener so it turns solid.
